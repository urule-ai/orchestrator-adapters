{
  "name": "@urule/goose-adapter",
  "version": "0.1.0-spike.0",
  "description": "Goose orchestrator adapter — bridges Urule to Goose daemon / Agent Client Protocol",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "license": "MIT",
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "@urule/orchestrator-contract": "file:../../orchestrator-contract",
    "fastify": "^5.0.0",
    "ulid": "^2.3.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
